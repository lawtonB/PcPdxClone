@{
    ViewData["Title"] = "Pc-Pdx";
    //Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = "";
}
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.0/jquery.min.js"></script>
@model PcPdx.Models.Show


<h4> Add a new Show</h4>
@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.ApplicationUserId)

    @Html.LabelFor(model => model.ShowTitle)
    @Html.TextBoxFor(model => model.ShowTitle)
    @*@Html.LabelFor(model => model.ApplicationUserId)
    @Html.TextBoxFor(model => model.ApplicationUserId)*@

    <input class= "new-show" type="submit" value="Add new show" />
}

<script type="text/javascript">
    $(document).ready(function () {
        $('.addShow').click(function () {
            $.ajax({
                type: 'GET',
                dataType: 'html',
                url: "Shows/Create",
                success: function (result) {
                    $('#showShow').html(result);
                }
            })
        });

        $('.new-show').submit(function (event) {
            event.preventDefault();
            console.log("start");
            $.ajax({
                url: "Shows/Create",
                type: 'POST',
                dataType: 'json',
                data: $(this).serialize(),
                success: function (result) {
                    console.log(result)
                }
            })
        })
    })
</script>
